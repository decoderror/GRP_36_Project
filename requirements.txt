# Dependencies for task1_oop_application (GUI)

pygame==2.6.1
pygame_gui==0.6.14

# Common transitive deps (usually installed automatically, pinned here for stability)
numpy>=1.26,<3
typing_extensions>=4.8
