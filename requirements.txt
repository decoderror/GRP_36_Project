# Dependencies for the Disaster Response Simulator (task1_oop_application)

pygame==2.6.1
pygame_gui==0.6.14

# Transitive dependencies (usually installed automatically, pinned for stability)
numpy>=1.26,<3
typing_extensions>=4.8
